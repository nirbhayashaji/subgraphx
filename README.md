# SubgraphX-GNN: Explainable Graph Anomaly Detection Pipeline

## Overview
This repository contains an end-to-end Machine Learning pipeline designed to detect and explain anomalous nodes (e.g., fraudulent entities, illicit networks) within complex transfer networks. 

It utilizes a multi-model ensemble approach—combining traditional density-based outlier detection with Graph Neural Networks (GNNs)—and leverages SubgraphX (Monte Carlo Tree Search + Shapley Values) to provide human-readable, forensic explanations of *why* the GNN flagged a specific node.

## Architecture & Process Flow
The pipeline operates in three distinct phases: Feature Engineering, Anomaly Detection, and Explainability/Visualization.

```text
[Raw Data] 
    │
    ├──> processing.py (Data Cleaning)
    │
    ├─ Phase 1: Feature Engineering 
    │    ├── graph_feature_creation.py (Structural Metrics: Centrality, Degree)
    │    └── transfer_feature_creation.py (Domain Metrics: Volume, Hazard Rates)
    │
    ├─ Phase 2: Anomaly Detection Ensemble (anomaly_engine.py)
    │    ├── Isolation Forest (IF)
    │    ├── Local Outlier Factor (LOF)
    │    ├── K-Means Clustering
    │    ├── GNN Autoencoder (model_ae.py)
    │    └──> Borda Count Aggregation (Calculates Overall Consensus Rank)
    │
    └─ Phase 3: Explainability & Visualization (explainer_logic.py)
         ├── SubgraphX MCTS (Isolates the anomalous subgraph/motif)
         ├── Shapley Value Calculation (Scores accomplice impact)
         └── visualization_logic.py (Generates Master Dashboards)

Usage

To run the full end-to-end pipeline and enter Interactive Investigation Mode: 
python main.py
To bypass ML training and generate explainability dashboards for known suspects directly: 
python phase_2_anomaly_detection/explainer_logic.py


---

### 2. Slide Deck Outline: "Understanding the Architecture"


#### **Slide 1: Pipeline Overview & Objectives**
*   **Headline:** Moving from Black-Box to Forensic Explainability
*   **Visual:** Use a simplified version of the flowchart from the README.
*   **Talking Points:** 
    *   This repository is an end-to-end pipeline designed to find illicit nodes in massive transaction networks.
    *   It solves the "black-box" problem: We don't just produce a risk score; we produce a visual explanation of the specific subgraph (accomplice network) driving that risk.
    *   The architecture is highly modular, split into Feature Engineering, Ensemble Detection, and MCTS Explainability.

#### **Slide 2: Dual-Track Feature Engineering**
*   **Headline:** Capturing both Geometry and Physical Domain Reality
*   **Visual:** Two icons feeding into a single master database icon. 
*   **Talking Points:**
    *   *(Referencing `graph_feature_creation.py`)*: We calculate structural topologies (PageRank, Triangles, Betweenness) to understand the entity's position in the network.
    *   *(Referencing `transfer_feature_creation.py`)*: We calculate physical domain metrics (Hazardous flow rates, individual vs. corporate supplier ratios) to ground the math in business reality. 

#### **Slide 3: The Consensus Engine (Phase 2)**
*   **Headline:** Mitigating Algorithmic Blindspots
*   **Visual:** A graphic showing four algorithms pointing to a single ranked list.
*   **Talking Points:**
    *   *(Referencing `anomaly_engine.py`)*: No single algorithm is perfect. LOF captures tabular density anomalies, while the GNN Autoencoder captures structural geometry. 
    *   By running an Isolation Forest, LOF, K-Means, and a GNN Autoencoder simultaneously, we use a Borda Count to find the absolute highest-confidence anomalies.

#### **Slide 4: SubgraphX Explainability (Phase 3)**
*   **Headline:** Isolating the "Crime Scene"
*   **Visual:** The SubgraphX Dashboard generated by `visualization_logic.py` (the network graph with the red/blue arrows and the intelligence profile text box).
*   **Talking Points:**
    *   *(Referencing `explainer_logic.py`)*: Once a node is flagged, SubgraphX uses Monte Carlo Tree Search to prune away normal connections.
    *   It isolates the exact "motif" (e.g., a 47-to-1 transfer ring) and uses Shapley Values to rank exactly which accomplices are cooperating to cause the anomaly.